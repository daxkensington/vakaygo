name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: node scripts/check-lint.mjs
      - run: npm run typecheck
      - run: npm run check:i18n
      - run: npm test -- --run
        env:
          NODE_ENV: test

  e2e:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: audit
          POSTGRES_PASSWORD: audit
          POSTGRES_DB: vakaygo_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U audit -d vakaygo_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://audit:audit@localhost:5432/vakaygo_test
      AUTH_SECRET: ci-only-secret-for-isolated-booking-tests-32chars
      RESEND_API_KEY: re_ci_only_no_live_email
    needs: check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - uses: ./.github/actions/prepare-test-db
      - run: npm run build
      - run: npx playwright test
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  lighthouse:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: audit
          POSTGRES_PASSWORD: audit
          POSTGRES_DB: vakaygo_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U audit -d vakaygo_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://audit:audit@localhost:5432/vakaygo_test
      AUTH_SECRET: ci-only-secret-for-isolated-booking-tests-32chars
      RESEND_API_KEY: re_ci_only_no_live_email
    needs: check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - uses: ./.github/actions/prepare-test-db
      - run: npm run build
      - name: Run Lighthouse CI
        run: |
          npm install -g @lhci/cli
          lhci autorun
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

  database:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: audit
          POSTGRES_PASSWORD: audit
          POSTGRES_DB: vakaygo_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U audit -d vakaygo_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://audit:audit@localhost:5432/vakaygo_test
    steps:
      - uses: actions/checkout@v4
      - name: Apply migrations to an empty isolated database
        run: |
          for migration in drizzle/migrations/*.sql; do
            psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$migration"
          done
      - name: Verify inventory and transactional notifications
        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f tests/sql/booking-integrity.sql
      - name: Verify concurrent reservation exclusion
        run: bash tests/sql/concurrent-booking.sh
